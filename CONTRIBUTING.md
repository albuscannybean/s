# Contributing

Create a focused branch, preserve the identity and relation invariants, add tests for domain changes, and run `npm test` plus the Windows publish command before opening a pull request. Never use titles as identifiers or encode semantic relations only as screen coordinates.
