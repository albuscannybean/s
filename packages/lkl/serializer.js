const q=value=>JSON.stringify(String(value??''));const j=value=>q(JSON.stringify(value??null));
export function serializeLkl(definition){const lines=['lkl 1',`structure ${q(definition.id)} ${q(definition.name)}`,`description ${q(definition.description??'')}`,`category ${q(definition.category??'custom')}`,`layout ${q(definition.layout?.type??'manual')} ${j(Object.fromEntries(Object.entries(definition.layout??{}).filter(([key])=>key!=='type')))}`,`visual ${j(definition.visual??{})}`,`flags ${j({nestable:definition.nestable??true,computable:definition.computable??false})}`];if(definition.slotFactory)lines.push(`factory ${q(definition.slotFactory)}`);for(const parameter of definition.parameters??[])lines.push(`parameter ${q(parameter.id)} ${q(parameter.label)} ${q(parameter.type)} ${j(parameter.defaultValue)} ${j(Object.fromEntries(Object.entries(parameter).filter(([key])=>!['id','label','type','defaultValue'].includes(key))))}`);for(const variable of definition.variables??[])lines.push(`variable ${q(variable.id)} ${q(variable.label)} ${q(variable.type??'number')} ${q(variable.kind??'input')} ${j(variable.value??null)} ${q(variable.formula??'')} ${j(variable.expression??null)}`);for(const slot of definition.slots??[])lines.push(`slot ${q(slot.id)} ${q(slot.label)} ${q(slot.role)} ${q(slot.cardinality??'one')} ${q((slot.accepts??[]).join(','))} ${j(slot.semanticCoordinate??{})}`);for(const edge of definition.edges??[])lines.push(`edge ${q(edge.id)} ${q(edge.sourceSlotId)} ${q(edge.targetSlotId)} ${q(edge.direction??'directed')} ${q(edge.relationType??'related')} ${q(edge.label??'')} ${q(edge.routing??'straight')} ${q(edge.semanticAxis??'')} ${j(edge.visual??{})}`);for(const constraint of definition.constraints??[])lines.push(`constraint ${j(constraint)}`);for(const rule of definition.rules??[])lines.push(`rule ${j(rule)}`);lines.push('end','');return lines.join('\n')}
